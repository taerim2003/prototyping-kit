# 2026-08-13

## 세션 1 — 블루베리 디펜스 (세션39, 8-12 밤부터 이어짐)

**목표**: "번역 시스템 만드는거 착수해줘."

**한 일**: Unity Localization 도입(패키지·Locale ko/en·StringTable `Game`), 조회 창구 `Loc.cs` 신설,
**하드코딩 스위치 테이블 7개(402건)를 키 조회로 교체**, 설정 패널에 언어 선택 행 추가, 수확·적재 에디터 도구 2종.
코드가 485줄 줄었다. 용어집(스킬·패시브·진화명 85개) 영어 초벌.

**과정 — 잘 된 것**:
- 🔴 **"546건 손으로 치환"이 아니라 "테이블 12개 교체"라는 걸 먼저 센 것.** 착수하자마자 치환에 들어갔으면
  파일 19개를 헤집었을 것이다. 형태별로 갈라 보니 **402건이 순수 switch 테이블**이었고, 그건 메서드 하나당 한 줄로 줄어든다.
  "얼마나 많은가"가 아니라 **"어떤 모양인가"**를 먼저 본 게 갈랐다.
- 🔴 **추출을 두 갈래로 짠 것.** 소스 정규식 파싱(385건)과 **Unity에서 함수를 실제로 호출한 수확**(386건)을 따로 만들어 대조했다.
  값까지 0 불일치였고, 차이 1건은 정규식이 못 잡는 `_ => "공격"` 기본 분기로 **설명이 됐다**.
  한쪽만 믿었으면 "잡히지 않은 것"을 영영 몰랐을 자리다.
- 🔴 **대조군을 돌려 검사가 살아 있는지 본 것.** 두 추출이 완전히 일치하자 그대로 믿지 않고, 값 1개를 바꾸고 키 1개를 지운
  사본으로 다시 돌려 **정확히 3건이 잡히는 것**을 확인했다. "전부 통과"는 의심 신호라는 규칙이 실제로 작동했다.
- 🔴 **치환 전 수확본을 오라클로 떠 둔 것.** 표 7개를 갈아엎은 뒤 같은 수확을 다시 돌려 **386/386 완전 일치**를 확인했다.
  "동작이 안 바뀌었다"를 말이 아니라 값으로 증명했다. 이게 없었으면 90줄짜리 switch 4개를 지우는 걸 눈으로 검수해야 했다.
- **오탐을 걷어낸 것.** 첫 집계 546건에는 **코드 뒤 주석 안의 따옴표**(`// 스킬이 "대공 불가라 못 맞힘" 판정에`)와
  `[ContextMenu]`, GameObject 이름이 섞여 있었다. 주석 제거 파서를 만들어 530건으로 정정했다.
  안 걷었으면 번역 대상이 아닌 것을 표에 넣고 있었을 것이다.
- **막힌 동안 Unity가 필요 없는 일로 옮겨간 것.** 플레이모드 때문에 에셋 생성이 막힌 30여 분을
  추출·키 설계·보간 변환기·용어집으로 채웠다. 플레이가 끝나자 바로 이어붙였다.

**과정 — 막힌 것 / 마찰**:
- 🔴 **플레이모드를 확인하지 않고 패키지부터 설치했다.** `Client.Add`는 성공했는데 컴파일이 안 돌아
  `Type.GetType`이 계속 MISS를 뱉었다. **원인을 찾는 데 호출 5번**을 썼다 — `assets-refresh`, `Client.Resolve`,
  `RequestScriptCompilation`, 어셈블리 열거까지 다 해 보고 나서야 `isPlaying=True`를 봤다.
  `unity-mcp` 스킬 체크리스트 2번("플레이 중엔 스크립트가 컴파일되지 않는다")이 정확히 이 상황인데 **뒤늦게 떠올렸다.**
  → 세션38 일기에 "씬을 고치기 전에 플레이모드를 확인한다"고 적어놓고, **패키지 설치는 그 범주로 안 읽었다.**
- 🔴 **Unity YAML이 한글을 `\uXXXX`로 이스케이프한다는 걸 몰라 씬 집계가 0으로 나왔다.** 그걸 그대로
  "씬에는 한글이 없다"고 읽을 뻔했다. `m_text:` 줄을 직접 찍어 보고서야 `뒤로`(뒤로)가 보였다.
  **집계가 0이면 대상이 없는 게 아니라 내 검사가 틀린 것일 수 있다** — 여기서도 "0은 의심 신호"였다.
- **`Loc.Locales`가 초기화를 보장하지 않는 버그를 내가 심었다.** `T()`에만 `EnsureInit`을 넣고
  언어 목록·전환 경로엔 빼먹었다. 설정 패널이 우연히 `Loc.T()`를 먼저 부르는 덕에 **가려져 있었다** —
  검증 로그에 `Locales.Count = 0`이 찍혀서 잡았다. 로그를 눈으로 훑지 않았으면 넘어갔을 자리다.
- **`initialized` 플래그가 한 번 서면 재초기화를 안 해, 제공자가 비워지면 복구 불가였다.**
  에디터에서 에셋을 저장하면 실제로 비워진다는 걸 `LocaleName` 갱신 중에 발견했다.

**AI 협업 체크** *(코드 버그 말고 과정 레벨로)*:
- **사용자가 내 추천과 다른 쪽을 고른 것 — Unity Localization 패키지.**
  나는 자체 경량 방식을 추천했다(600건 중 씬 텍스트가 28건뿐이라 패키지의 강점이 안 산다는 이유).
  사용자가 표준 패키지를 골랐고 그대로 갔다. **결과적으로 폴백·언어 전환·에디터 표를 공짜로 얻었다** —
  내 논거는 "배선 비용"만 봤고 **"나중에 손댈 사람"** 은 안 봤다. 도구 선택에서 내가 자주 놓치는 축이다.
- **선택지를 낼 때 preview로 코드 모양을 나란히 보여준 것.** 세 방식의 실제 호출 코드와 파일 구조를
  한 화면에 놓으니 한 번에 결정이 났다. 세션38의 "후보를 나란히 렌더해 고르게 한다"가 코드 선택에도 먹혔다.
- **미리 짚은 것 — 영어가 한국어보다 길다.** 진화·레벨업 카드 폭이 고정이라 넘칠 수 있다는 걸
  번역을 시작하기 전에 알렸다. 세션38에 "캔버스 크기 변경의 파급을 사용자가 그림을 다 그린 뒤에 짚었다"는
  마찰이 있었는데, 이번엔 **작업 전에** 짚었다.
- **자율적으로 잘 결정한 것** — `Assets/Sound/`(164MB)를 커밋에서 뺐다. 사용자가 "정리해"라고 했지만
  HANDOFF에 "사용자 결정 대기 중"이라 적혀 있었고, git 히스토리는 되돌리기가 가장 비싼 축이다.

**다음에 바꿀 것**:
- **[→ Kit] "플레이모드 확인"은 씬 편집만이 아니라 *컴파일이 필요한 모든 작업*의 선행이다.**
  트리거 자명 — *패키지 설치·`.cs` 신설·`assets-refresh` 등 **재컴파일을 기대하는 순간**.*
  `EditorApplication.isPlaying`을 **한 번 찍는 비용은 진단 5회보다 싸다.** 지금 스킬에는 "플레이모드로 측정할 때"
  체크리스트 안에만 있어서 **측정이 아닌 작업엔 발동을 안 했다.**
- **[→ Kit] 집계가 0으로 나오면 "대상이 없다"가 아니라 "검사가 틀렸다"를 먼저 의심한다.**
  대조군 규칙("전부 통과는 의심 신호")의 반대쪽 얼굴인데, 그 규칙은 **검사 결과**만 말해서 **집계 0**엔 안 걸렸다.
  이번에 Unity YAML의 `\uXXXX` 이스케이프가 정확히 그랬다. **알려진 양성 표본 하나로 검사를 먼저 때려볼 것.**
- **[→ Kit] 래퍼를 만들면 "느린 경로"뿐 아니라 *모든 공개 진입점*에 같은 전제를 건다.**
  `Loc.T()`에만 초기화를 넣고 `Loc.Locales`엔 빼먹은 게 그것이다. 진입점을 열거하고 전제를 표로 맞출 것.

**메모**: 세션 시작 시 Unity가 플레이 중이었고, 그 덕에 세션38의 확인 항목(`pixelroborobo SDF` Dynamic 아틀라스 채우기)이
**저절로 소화됐다** — `git status`에 폰트 에셋 +395줄로 나타났다. 내 작업이 아닌 줄을 갈라내는 데 세션 시작 스냅샷이 또 근거가 됐다.

---

## 세션 2 — 블루베리 디펜스 (세션40)

**목표**: "번역 시스템 안 된 거 있으면 마무리해."

**한 일**: INLINE 문자열 133건 외부화 · `LocalizedTmp` 신설 + 씬 TMP 15개 배선 · 스킬트리 노드/캐릭터/맵 74건을
`Loc.TOr` 창구로 · 적재 도구를 `모든 TSV를 표에 적재` 하나로 통합 · **영어 590건 초벌**(표 ko 602 / en 602).

**과정 — 잘 된 것**:
- 🔴 **스캔이 0으로 나왔을 때 "대상이 없다"로 안 읽은 것.** 세션39에 [→ Kit]으로 적어둔 규칙이 **바로 다음 세션에** 발동했다.
  한글 리터럴 스캔이 `TOTAL=0`을 뱉었는데, grep 대조군이 330건을 보여줘서 즉시 내 검사가 틀렸음을 알았다.
  원인은 PS 5.1이 BOM 없는 UTF-8 스크립트의 한글 정규식을 깨뜨린 것. **규칙을 적어둔 값을 그 자리에서 받았다.**
- 🔴 **자리표시자를 전수 대조한 것.** ko/en 602건의 `{N}` 최대 인덱스를 비교했다 — 하나만 어긋나도 화면에 `{0}`이 그대로 뜬다.
  `MaxIdx`가 실제로 판별하는지 대조군까지 돌려 "0건 불일치"가 무력한 통과가 아님을 같이 증명했다.
- 🔴 **"영어가 길어 넘친다"를 손계산으로 닫지 않고 실측한 것.** 카드에 🔴로 적혀 있던 위험인데, 이름·제목 263건을
  전각2/반각1로 재니 **en 최대 25 < ko 최대 29** — 레이아웃이 이미 감당하는 폭이었다. **위험 자체가 낮았다.**
  안 쟀으면 카드 폭을 손보는 헛일을 했을 수 있다(세션38 "손계산으로 가능성을 닫지 말 것"의 반대 방향).
- 🔴 **"씬 TMP 28건"을 그대로 배선하지 않은 것.** 각 TMP가 런타임에 코드로 덮어써지는지 grep했더니 **13개가 플레이스홀더**였다.
  전부 붙였으면 언어 전환 시 계산된 값("1250 정수")을 정적 문구로 되돌리는 **버그**가 됐다. 카드의 숫자는 대상 수지 작업 목록이 아니었다.
- **사용자 질문에 코드로 답하지 않고 돌려본 것.** "실제로 언어를 바꿀 수 있어?"에 경로만 읽고 "됩니다"라 할 수 있었는데,
  플레이모드로 돌리니 `Rebuild()`의 **1프레임 캔버스 중복**이 보였다(다음 프레임에 누수 0까지 확인). 값만 봤으면 못 봤다.

**과정 — 막힌 것 / 마찰**:
- 🔴 **PS 5.1 + BOM 없는 UTF-8 함정에 한 세션에 두 번 걸렸다.** 스캔 스크립트(한글 정규식)와 cmap 스크립트(한글 출력 문구).
  스킬 두 곳에 적혀 있는 내용인데 **둘 다 "파일을 왕복시키지 말 것"으로 적혀 있어** 스크립트를 *쓸* 때는 발동을 안 했다.
- **집계 숫자를 커버리지로 읽었다.** `harvest_en 신규 385` vs `harvest_ko 386`을 보고 "한 건 빠졌다"고 사용자에게 보고했는데,
  키 집합을 직접 diff하니 **0이었다**(이미 en에 있던 1건이 "신규"에서 빠졌을 뿐). **집계와 집합은 다르다.**
- **3자 표본으로 폰트 커버리지를 판정했다가 틀렸다.** "한글 1자 누락"이 떴는데 전수(11172자) 스윕은 **100%**였다.
  표본이 오답을 냈고 그대로 보고할 뻔했다. 전수 스윕은 1초도 안 걸렸다 — 표본을 쓸 이유가 없었다.

**AI 협업 체크** *(코드 버그 말고 과정 레벨로)*:
- **사용자가 내 권장보다 넓은 쪽을 골랐다 — 영어 590건 전부.** 나는 "UI 146건만 먼저"를 권했다(용어집 검수가 선행이라
  뒤집히면 진화명 68개를 다시 손봐야 한다는 이유). 사용자가 전부를 골랐고, 결과적으로 **표가 꽉 차서 en으로 한 판 돌려볼 수 있는 상태**가 됐다.
  내 논거는 **되돌리기 비용**만 셌고 **"확인 가능한 상태가 되는 것"의 가치**는 안 셌다.
  세션39에도 같은 축을 놓쳤다(패키지 선택에서 "나중에 손댈 사람"을 안 봄). **두 세션 연속 같은 종류의 누락이다.**
- **범위가 갈리는 자리에서 물은 것.** 영어를 지금 채울지는 "어느 쪽이어도 일이 되지만 결과가 크게 다른" 갈림길이라 물었고,
  배선(A~D)은 선언하고 바로 진행했다. 물을 곳과 선언할 곳을 가른 게 왕복을 줄였다.
- **`/wrap`을 내가 실행하려다 도구 수준에서 막혔다.** "마무리해"를 세션 종료로 읽고 Skill 툴로 호출했는데
  `disable-model-invocation`이었다. **부수효과 있는 루틴은 사용자만 친다**는 규칙이 문서가 아니라 도구로 지켜졌다 — 좋은 배선이다.

**다음에 바꿀 것**:
- **[→ Kit] PowerShell 스크립트를 *만들 때* 그 안에 한글 리터럴을 쓰지 말 것.**
  트리거 자명 — *Write 툴로 `.ps1`을 만드는 순간.* 정규식은 `[char]0xAC00` 식으로 **char 코드로 조립**하고 출력 문구는 **ASCII로**.
  기존 규칙("파일을 PowerShell로 왕복시키지 말 것")은 **읽기/쓰기**만 말해서 **스크립트 소스 자체의 한글**엔 발동을 안 했다.
- **[→ Kit] 집계 숫자(`신규 N건`)를 커버리지 근거로 쓰지 말 것.** 커버리지는 **집합을 직접 대조**해야 나온다.
  트리거 자명 — *도구가 뱉은 개수로 "다 됐다/하나 빠졌다"를 말하려는 순간.*
- **[→ Kit] 전수 조사가 가능하면 표본을 쓰지 말 것.** 표본이 오답을 내면 그걸 **사실로 보고하게 된다.**
  트리거 자명 — *"대표 몇 개만 찍어 보자"고 생각하는 순간.* 전체 크기를 먼저 세고, 감당되면 전부 돈다.

**메모**: 19:21~19:40에 **병렬 UI 스킨 세션**이 돌아 `EvolutionTreeUI`·`PauseMenu`에 내 번역 편집과 섞였고
`UISkin.cs`·`UISkinApply.cs`·`UISkin.asset`·`SkillNode.prefab`이 새로 들어왔다. 세션 시작 `git status` 스냅샷과
**파일 수정 시각**으로 갈랐다 — 스냅샷만으로는 부족했고 타임스탬프가 결정적이었다.
