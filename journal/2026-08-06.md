# 2026-08-06

## 세션 1 — 블루베리 디펜스 (세션 32)

**목표**: `brief`로 후보 4개를 뽑았지만 실제로는 **사용자 요청 흐름**으로 갔다 — 버튼 버그 2건 → 노션 문서화 2건 → 패시브 "방어" 신설.

**한 일**:
- **호버 떨림 수정** — `visualRoot`에 버튼 자신을 넣어 쓴 탓에 7도 회전이 히트박스를 통째로 돌려 `Exit ↔ Enter`가 무한 반복됐다. "회전 중 Exit는 미뤘다가 회전이 0으로 돌아온 뒤 한 번만 재판정"으로 해결.
- **버튼 피벗 19개 전수 조사 → 8개 교정**(끝 피벗 → 중앙). 런타임 생성 5개는 배치 뒤 `JuicyTuning.CenterPivot()`.
- **노션 문서 2개** — 진화 피드백 수집본(스킬 단위 재배열 + 피드백 칸 14개) + **스킬 데이터 시트**(전 스킬 수치·커브·만렙 누적·진화 조건 mermaid 다이어그램).
- **패시브 "방어" 신설** — 받는 피해 비율 감소(만렙 42%). 기본만 구현, 진화는 다음 세션.
- **일정 정정** — HANDOFF의 폴리싱 마감이 8/7로 틀려 있었다(캘린더 원본 8/9).

**과정 — 잘 된 것**:
- 🟢 **대조군을 모든 검증에 넣었고, 그게 전부 값을 했다.** 피벗 교정은 "보정 안 했으면 29~119px 이동 / 실제 0px", 호버 굳음은 "수정 전=안(굳음) / 수정 후=밖(해제)", 방어는 "방어 0%면 원래 피해 그대로". **셋 다 검사가 무력하지 않다는 걸 같이 증명**해서, "전부 통과"가 의심 신호가 되지 않았다.
- 🟢 **"에셋이 정답"이라는 규칙이 실제로 문서를 구했다.** 스킬 시트를 만들며 코드 폴백(화살 16·휘두르기 20)과 에셋(6·12)이 **다르다**는 걸 봤다. 코드만 읽고 썼으면 전 수치가 틀린 문서가 됐다.
- 🟢 **모순을 그냥 넘기지 않은 게 큰 걸 잡았다.** 첫 시뮬레이션에서 낙뢰 피해가 0으로 나왔는데 "게임에서 낙뢰는 피해를 준다"와 안 맞았다. 초기화 경로를 추적하니 `GetDefaultDamage()`가 **낙뢰만 다른 상수**(`LightningStorm.BaseProcDamage`)를 읽고 있었고, `Prog_Lightning.baseDamage`는 **아무도 안 읽는 값**이었다. 시뮬레이션을 게임의 실제 경로로 다시 짰다.
- 🟢 **"이미 있는 곳부터 센다"가 두 번 걸렀다.** ① 패시브 후보가 `LevelUpUI`에 **하드코딩 배열**이라 거기 안 넣으면 코드는 다 맞는데 게임에만 안 뜬다. ② 방어 진화로 "반격"을 떠올렸는데 **건강 R1이 이미 반격**이었다.
- 🟢 **넣기 전에 안 넣었을 때의 사고를 봤다.** 진화 없이 패시브를 넣으면 `IsRouteUnlocked`가 연계 조건 null을 "조건 없음=열림"으로 봐서 **이름도 효과도 그대로인 빈 진화**가 뜬다는 걸 코드로 확인하고 가드를 넣었다.

**과정 — 막힌 것 / 마찰**:
1. 🔴 **내가 고친 게 새 버그를 만들었고, 그 지점을 내가 이미 지목해 놓고도 검증을 안 했다.** 떨림 수정 후 "**호버가 굳어 큰 채로 남지는 않는지** — 이론상 재판정이 잡지만 실제 확인이 필요한 유일한 지점"이라고 써서 사용자에게 넘겼는데, **정확히 거기가 터졌다**(재판정을 확대된 rect로 해서 원본 450~확대 517.5 사이 띠에서 굳음). 나중에 재현 스크립트를 3줄로 짰다 — **처음부터 짤 수 있었다.**
2. 🔴 **일정을 HANDOFF 사본으로 읊었다.** 폴리싱 마감을 8/7로 브리핑했는데 사용자가 *"내일 마감이 아니라 주말까지일 텐데 캘린더 확인해봐"* 라고 잡아줬다. 캘린더 원본은 **8/9**. `brief` 2단계에서 "다음 할 일"은 코드로 대조했지만 **일정 표는 대조 대상에 아예 안 넣었다.**
3. **`AskUserQuestion`이 JSON 파싱 실패**로 죽었다(한글 + 긴 preview). 텍스트 선택지로 우회.
4. **`/wrap`을 Skill 툴로 호출하려다 막혔다** — `disable-model-invocation`이라 사용자가 직접 쳐야 한다.
5. **노션 마크다운 표 두 개가 하나로 병합됐다**(휘두르기 판정 파라미터 + 진화 표). `update_content`로 `</table>` 삽입해 분리.
6. **PowerShell `$pid`가 예약 변수**라 스크립트가 통째로 실패. 변수명만 바꿔 해결.
7. **`script-execute`의 임시 오브젝트가 씬 dirty 플래그를 남겼다.** 내용은 디스크와 동일하지만, 사용자 미저장 편집이 섞였을 수 있어 **클리어하지 않고 알렸다.**

**AI 협업 체크** *(코드 버그 말고 과정 레벨로)*:
- *추측으로 진행한 것*: **있다.** 방어 패시브를 받자마자 **진화 4종까지 설계해 제안**했는데 사용자가 *"진화는 다음 세션"* 이라며 잘랐다. 요청은 "패시브 추가"였고 진화는 내가 얹은 범위다. 콘텐츠 동결 원칙은 짚었지만, 그 뒤 범위를 스스로 넓혔다.
- *사용자가 잡아준 것*: **두 건 다 내 검증 공백이었다.** ① 일정(위 마찰 2) ② 호버 굳음(위 마찰 1). 특히 ②는 **내가 위험하다고 써 놓은 자리**라 더 뼈아프다.
- *잘 결정한 것*: 낙뢰 모순을 추적해 시뮬레이션을 게임 실제 경로로 다시 짠 것. 손계산이나 에셋 값 직독이었으면 틀린 수치가 문서에 박혔다.
- *잘 결정한 것 2*: 방어 진화가 없는 상태의 **빈 진화 노출**을 미리 막은 것. 요청 범위 밖이었지만 안 막으면 다음 세션이 버그로 만난다.

**다음에 바꿀 것**:
- **[→ Kit] `brief`의 대조 대상에 "일정·날짜"를 넣는다.** 2단계는 지금 *미구현을 주장하는 항목*만 대조하는데 **날짜도 똑같이 낡는다**. 오늘 실제로 틀린 마감일을 브리핑했다. 트리거 자명 — *브리핑에 일정을 넣을 때*. (`brief` 스킬에 반영 완료)
- **[→ Kit] "확인 부탁"으로 넘기려는 위험이 *내가 만든 것*이면, 넘기기 전에 재현부터 해 본다.** 오늘 굳음 버그는 위험 지점을 정확히 지목해 놓고 검증 없이 넘겼다. **지목할 수 있으면 재현 조건도 짤 수 있다.** 트리거 자명 — *내 수정에 대해 "확인 부탁드립니다"를 쓰려 할 때*. (`unity-mcp` 스킬 검증 절에 반영 완료)

**메모**:
- 스킬 데이터 시트는 **리플렉션으로 게임의 실제 초기화·레벨업 경로를 실행**해 만들었다. 다음에 수치 문서가 필요하면 같은 방식이 정확하다(손계산 금지).
- 노션은 **mermaid 코드블록을 렌더링**한다. 마크다운 표도 네이티브 표로 변환돼 셀 편집이 된다 — 피드백 수집용 문서에 쓸 만하다.
- 진화 조사 문서의 등급 집계가 개별 판정과 안 맞았다(🔴8 vs 실제 9). 원문을 고치지 않고 **병기**해 뒀다.
- 방어의 "초반 6%는 잔몹에 효과 0"은 사용자가 **수용**으로 결정(정수 반올림). 버그로 오해하지 않게 HANDOFF·칸반 카드 양쪽에 적었다.

---

## 세션 2 — 블루베리 디펜스 (세션 33)

**목표**: 사용자가 노션 스킬 데이터 시트에 만든 **NEW 진화조건 표**를 검토하고 구현.

**한 일**:
- **연계 조건을 루트 좌표에서 떼어냈다** — 예전엔 `RoutePath`가 곧 조건이었는데(path1=패시브·path2=액티브·path0=무조건) NEW 표가 그 틀을 셋 다 깼다: 두 루트가 **둘 다 액티브** · **둘 다 패시브** · **R0=액티브/R1=패시브 반대 배치**. `EvolutionRoutes.RoutePrereq`가 `(스킬,루트) → 조건`을 단독 소유하게 바꿨다.
- **조건 없는 루트 10개 → 0개.** 신규 4종의 연계 조건 부재 해소.
- **패시브 "가속" 신설**(전 스킬 쿨감, 만렙 27.5%) · **리프레쉬 폐지**(후보 배열에서만) · **아이콘 2종 배선**(사용자 아트).

**과정 — 잘 된 것**:
- 🟢 **"표를 그대로 넣을 수 있는가"를 먼저 물은 게 이번 작업을 살렸다.** 표만 보고 값을 옮겼으면 **조건 슬롯이 스킬당 1+1개뿐**이라 절반이 조용히 안 들어갔을 것이다. 구현 전에 *구조가 이 데이터를 표현할 수 있나*를 확인한 것이 계획의 1단계가 됐다.
- 🟢 **손대는 범위를 좁게 잡은 판단이 맞았다.** 조건과 효과가 원래 분리돼 있어서 `RoutePath`·`PathTier[3]`·효과 코드 115군데를 **안 건드리고** 조건 테이블만 갈아끼웠다.
- 🟢 **대조군이 또 값을 했다.** 옛 규칙을 검산 스크립트 안에 재현해 "옛 규칙이었다면 나왔을 값"을 같이 찍었더니 **22루트가 다르다**고 나왔다 — 32/32 통과가 "검사가 무력해서 통과한 것"이 아님을 같은 출력에서 증명했다.
- 🟢 **"이미 있는 곳부터 센다"가 사용자 질문을 앞질렀다.** 아이콘을 넣기 전에 **액티브 10 + 패시브 7 전부를 감사**했더니 빠진 게 정확히 둘이었고, 마침 사용자가 *"지금 없는 아이콘 있니?"* 라고 물어 바로 답이 됐다.
- 🟢 **사용자 아트는 파일 수정 시각부터 봤다**(20:44·20:46 = 방금 그린 것). 임포트도 규칙대로 잡았다 — 둘 다 PPU 100·Bilinear·Multiple·Compressed로 들어와 있었다.

**과정 — 막힌 것 / 마찰**:
1. 🔴 **코드에 `case`가 있다는 걸 "게임에 그 기능이 있다"로 읽었다.** 리프레쉬 폐지의 파급으로 *"스킬트리 노드 `refresh_bonus`가 죽는다"* 를 보고하고 **그 전제로 사용자에게 질문까지 했는데**(답: "가속에 붙인다"), 나중에 트리 에셋을 열어 보니 **현행 `MainSkillTree.asset`에 그 노드가 애초에 없었다**(백업본에만 있음). `Meta.cs`의 `case "refresh_bonus"`만 보고 **그 스위치를 켜는 에셋을 안 봤다.** 질문이 헛돌았다.
2. 🟡 **계획서에 "변경 21루트"라고 썼는데 실제 22였다.** 손으로 셌다. 검산 스크립트가 잡아줬지만, 셀 수 있는 건 처음부터 세게 했어야 한다.
3. `ToolSearch`의 `select:`에 **`mcp__` 접두사가 필요**한 걸 몰라 첫 호출이 빈손으로 왔다.
4. 이 환경엔 **python이 없다** — 노션 검색 덤프(145KB) 파싱을 안내받은 python 대신 `grep`으로 우회했다.

**AI 협업 체크** *(코드 버그 말고 과정 레벨로)*:
- *추측으로 진행한 것*: **있다.** 위 마찰 1 — 노드의 존재를 확인하지 않고 "죽는다"고 단정한 뒤 **그걸 전제로 선택지를 만들었다.** 확인은 `grep` 한 번이면 됐다.
- *사용자가 잡아준 것*: 없음. 방어의 ESC 요약 누락은 **내가 먼저 보고**했고 사용자가 "넣어"로 결정했다.
- *잘 결정한 것*: 진화 효과 15종을 **범위에서 빼고 물어본 것.** 세션32에 "방어 진화까지 설계해 제안했다가 잘린" 마찰이 있어서, 이번엔 조건표까지로 끊고 확인했다.
- *잘 결정한 것 2*: 옛 `Get*Prereq`를 지운 것 — **내 변경이 만든 orphan**이라 §3 규칙에 맞고, 조건이 두 곳에 남아 갈라지는 걸 막았다.

**다음에 바꿀 것**:
- **[→ Kit] "코드에 스위치가 있다"와 "게임에 그 기능이 있다"는 다르다.** 스킬트리 노드·SO·씬 배열처럼 **에셋이 켜야 도는 것**은 코드만 보고 현황을 말하면 틀린다. CLAUDE.md의 *"밸런스 수치는 문서가 아니라 에셋이 정답"* 과 같은 함정인데, 그 규칙이 **수치**만 말하고 있어서 **기능의 존재 여부**엔 적용을 못 했다.
  트리거 자명 — *코드의 `case`·필드를 근거로 사용자에게 현황을 보고하거나 질문을 만들 때, 그 스위치를 켜는 에셋을 `grep`으로 한 번 확인한다.* (양쪽 CLAUDE.md에 반영 완료)

**메모**:
- 조건표는 `(스킬, 루트) → 조건` 표 하나라 이제 **어떤 배치든 표현된다.** 되돌리거나 다시 바꿀 자리도 그 한 곳뿐.
- 진화 효과 명세 15종은 **1차/2차 구분이 없다.** 다음 세션 착수 전에 정해야 한다(칸반 카드 본문에 적어 뒀다).
- 노션 카드 본문을 `update_content`로 고칠 때, 기존 본문에 **깨진 글자**("뜼다")가 섞여 있어 `old_str` 매칭이 까다로웠다. 짧은 조각으로 나눠 매칭하면 된다.

---

## 세션 3 — 블루베리 디펜스 (세션 34)

**목표**: "나머지 피쳐 구현" — 노션 명세 16종을 진화 효과로 전부 구현.

**한 일**:
- **패시브 6종 + 액티브 10종 = 16종 구현.** 방어·가속 진화 4종(세션33에서 넘어온 숙제)이 여기 포함돼 방어 카드가 닫혔다.
- **`HasEvolutionDesign` 가드 제거** — 세션32에 "빈 진화를 막으려고" 넣었던 것이 역할을 다했다.
- 피뢰침 기둥을 **임시 프리미티브**로 세웠다(사용자 요청, "아무렇게나").

**과정 — 잘 된 것**:
- 🟢 **착수 전에 갈림길 2개를 물어본 것이 이번 작업의 형태를 정했다.** 명세 불릿은 하나씩인데 진화는 루트당 2티어라 **1차/2차 구분이 없었고**, 32루트 중 16개만 명세가 있었다. 둘 다 물어서 "불릿=1차, 나머지 16루트는 안 건드림"으로 **범위가 닫혔다** — 안 물었으면 32루트를 다 만지려 들었을 것이다. (세션33의 "표를 그대로 넣을 수 있는가"와 같은 종류의 질문이었다.)
- 🟢 **손으로 셀 뻔한 것을 값으로 셌다.** 16개 진화가 각각 어느 `path`에 붙는지는 "R0=path1, R1=path2" 같은 규칙이 스킬마다 달라서 틀리기 딱 좋았다(틀리면 **조용히 안 켜진다**). 세션33에서 "21루트 vs 실제 22"로 틀렸던 게 있어 이번엔 처음부터 스크립트로 16개를 `RoutePath`와 대조했다. 전부 일치.
- 🟢 **대조군을 4개 검사에 전부 넣었다.** 특히 텍스트 검사(64/64 통과)는 그냥 두면 "무엇을 넣어도 통과"일 수 있어서, **미사용 좌표가 빈칸으로 나오는 것**을 같은 출력에 찍었다.
- 🟢 **파라미터가 6개로 불어난 자리에서 멈추고 구조를 바꿨다.** 회오리 "소멸 시 미니 소환"을 `SpawnWhirlwind`에 인자로 넘기다 6개가 됐는데, 반환값을 `Whirlwind`로 바꾸고 콜백을 배선하는 쪽으로 갈아엎었다. 코드가 짧아졌다.
- 🟢 **임시 프리미티브도 되읽어 검증했다.** "아무렇게나"라는 요청이었지만 촉의 월드 크기(0.56×0.56 = 정사각형)와 위치(y=2.2 = 기둥 꼭대기 정확히)를 값으로 확인했다 — 부모의 **비균등 스케일**이 자식에 곱해져 찌그러지는 건 눈으로 보기 전엔 모른다.

**과정 — 막힌 것 / 마찰**:
1. 🟡 **명세를 따르니 기존 기능 4개가 죽었는데, 이걸 구현 중에야 하나씩 발견했다.** 오브 제단·초과 피해 연쇄·체인 라이트닝·회오리 타겟팅. 명세는 "이 루트를 이렇게 바꿔라"만 말하고 **그 자리에 뭐가 있었는지는 안 말한다.** 착수 전 매핑표에 "기존 진화" 열을 만들긴 했는데, 거기 적은 건 **이름**("오브 제단")이지 그게 **딸린 코드 자산**(프리팹·VFX·전용 쿨타임 분기)이라는 건 안 봤다. 오브 제단은 `TryUseSkill`에 **전용 쿨타임 분기**까지 있어서, 그걸 안 지웠으면 오브R1이 조용히 15초 쿨로 돌 뻔했다.
2. 🟡 **`TodoWrite`가 한글 이스케이프로 두 번 죽었다**(JSON 파싱 실패). 한글을 그대로 넣으니 통과. 세션32의 `AskUserQuestion` 실패와 같은 계열.
3. 커밋 직전 `git status`에 **사용자가 세션 중에 그린 아트 22개**가 있었다. 물어보고 코드만 커밋했다.

**AI 협업 체크** *(코드 버그 말고 과정 레벨로)*:
- *추측으로 진행한 것*: **거의 없다.** 명세에 없는 판단(화살 UFO 버그를 같이 고칠까)이 떠올랐을 때 **넣지 않고 보고만** 했다 — HANDOFF에 "사용자 결정 필요"로 이미 적혀 있던 건이라 범위를 넓히는 게 됐을 것이다(세션32의 "방어 진화까지 설계했다가 잘린" 마찰의 반대 선택).
- 다만 **화살비의 `CanHitFlying=true`는 내가 정했다.** 새로 만드는 코드의 기본값이라 안 정할 수가 없었지만, 결과적으로 그 버그의 **유일한 우회로**가 됐다 — 보고했다.
- *사용자가 잡아준 것*: 없음. 아트 커밋 범위는 내가 먼저 물었다.
- *잘 결정한 것*: 죽은 기능 4개를 **지우지 않고 주석 달아 남긴 것.** 되살리는 법까지 적었다. 다만 `AssassinateWhirlwindCritBonus`는 읽는 곳이 0이 돼서 지웠다 — 남기는 것과 지우는 것의 기준을 "읽는 코드가 남아 있는가"로 잡았다.

**다음에 바꿀 것**:
- **[→ Kit] 기존 동작을 *교체*하는 작업은, 교체 대상에 딸린 코드 자산을 먼저 센다.** 명세는 "무엇으로 바꿀지"만 말하고 "무엇이 사라지는지"는 안 말한다. 오늘 4개가 죽었고 그중 하나(오브 제단)는 **다른 함수에 숨은 분기**까지 있었다 — 안 지웠으면 새 진화가 조용히 15초 쿨로 돌았다.
  트리거 자명 — *"A를 B로 바꾼다"는 요청을 받으면, B를 짜기 전에 **A를 `grep`해 호출부·전용 분기·프리팹 참조를 전부 열거**한다.* CLAUDE.md §1의 *"넣어줘를 받으면 이미 있는 곳부터 센다"* 와 짝이다 — 그건 **더할 때**를 말하고 이건 **갈아끼울 때**다. (양쪽 CLAUDE.md에 반영 완료)

**메모**:
- 진화 1회가 `PathTier`를 곧바로 **2**로 만든다(1차=2, 2차=3). 그래서 새로 쓰는 실시간 분기는 **`>=2` / `>=3`**으로 읽어야 한다 — `>=1`은 1차만 해도 참이라 "2차 전용"을 표현하지 못한다. 이번에 새로 쓴 분기는 전부 이 규칙을 지켰다.
- `Projectile`은 자기 **로컬 left**로만 날아간다 → 방향을 주려면 `Quaternion.Euler`로 회전시키면 된다. 스프라이트도 같이 기울어 그림이 저절로 맞는다(화살비·추적 화살 둘 다 이걸로 해결, 클래스 수정 0줄).
- 사용자가 세션 중에 진화 아이콘 20종을 전부 그렸는데 **받는 필드가 코드에 아예 없다**. 다음 세션 카드로 만들어 뒀다.
