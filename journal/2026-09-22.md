# 2026-09-22

## 세션 1 — 블루베리 디펜스 (Steam 업적)

**목표**: "정상 클리어하면 전부 깰 수 있는" 도전과제 계획 → 구현.

**한 일**: 콘텐츠 전수 조사(진입점 배열 기준) → 봇 완주 기록 50회로 기준값 실측 → 업적 26개 설계·코드 배선(`Achievements.cs` 창구 + 훅 한두 줄씩, 세이브 소급 판정). 오프라인 컴파일 통과. 칸반 「Steamworks에 도전과제 등록하기」 카드 생성. **커밋은 보류**(병렬 세션이 같은 파일 작업 중).

**과정 — 잘 된 것**:
- 기준값을 감으로 안 잡고 봇 완주 로그(`runs.jsonl`)를 캠페인 단위로 묶어 쟀다 → "2차 진화는 50회 중 21회가 엔딩까지 안 나온다"가 드러났다. 설계 원칙("저절로 받는다")을 깨는 항목을 구현 전에 잡았다.
- 해금 조건(정수·맵)을 업적 코드에 복사하지 않고 로스터 에셋(`CharacterDefinition.IsUnlocked`)에 직접 물었다 — 사본이 안 생긴다.

**과정 — 막힌 것 / 마찰**:
- 노션 카드 생성에 한글을 유니코드 이스케이프로 보냈다(kanban 스킬이 금지한 것). 되읽기 단계에서 이상 없음을 확인했지만, 규범은 또 안 먹혔고 **되읽기 단계가 유일한 방어선**이라는 게 다시 확인됐다.
- Python이 없는 환경에서 치환 스크립트를 짜서 한 번 날렸다(Edit 툴로 재작업).
- 병렬 세션이 "어머니→군집체" 일괄 치환을 하면서 내가 쓴 HANDOFF 문장까지 바꿔 뜻이 뒤집혔다("주석의 '어머니'는 그대로 둠" → "'군집체'는 그대로 둠"). wrap에서 발견해 고쳤다.
- wrap의 커밋 단계가 병렬 세션과 부딪혔다 — 같은 파일에 남의 미완성 변경이 섞여 있어 커밋 불가.

**AI 협업 체크**:
- 사용자는 "계획을 세워줘"라고 했는데, 계획 승인 뒤 그대로 구현까지 갔다. 결과적으로 사용자가 이어서 요청을 줬지만(엘리트 삭제·군집체 문구), 범위가 한 단계 넓어진 건 맞다.
- 업적 이름·영어 문구 초안은 내가 지었다(Steamworks 입력 전에 사용자가 고칠 것).
- 자율 결정 중 괜찮았던 것: 병렬 세션 busy를 `ListAgents`로 확인하고 커밋을 보류한 것.

**다음에 바꿀 것**: 병렬 세션이 여럿 도는 날엔 wrap 커밋을 "마지막에 끝나는 세션"이 몰아서 하게 된다 — 지금은 HANDOFF에 "커밋 안 됨 + 내 파일 목록"을 남기는 것으로 넘긴다(규칙화는 한 번 더 겪으면).

**메모**: 에디터·봇에선 Steam이 꺼져 `[ACH]` 로그만 찍힌다. 실제 팝업 검증은 Steamworks 등록 뒤 베타 브랜치 빌드로.

## 세션 2 — 블루베리 디펜스 (스킬트리 툴팁 글자 위치)

**목표**: 노드 호버 툴팁의 글자가 판 위로 삐져나오는 것 고치기.

**한 일**: 판 그림(`개큰네모_색칠`) 색·알파를 열별로 스캔해 밝은 면(위 46~200px)을 잼 → `Tooltip`의 Name·Desc·Cost 칸을 그 안으로 옮김 → 가장 긴 이름·설명으로 캡처 확인 → 에디트모드에서 씬 저장. 커밋은 `Title.unity`에서 **내 hunk 3개만** 패치로 스테이징(같은 파일에 병렬 세션의 크레딧 변경이 섞여 있음).

**과정 — 잘 된 것**:
- CLAUDE.md §5-1 "판 실루엣을 PNG로 재서 배치"를 그대로 따랐더니 한 번에 맞았다. 가장 긴 문구로 찍은 게 최악 조건 검증이 됐다.
- 같은 파일에 병렬 세션 변경이 섞여 있어도 `git apply --cached`로 내 hunk만 골라 커밋할 수 있었다 — 세션 1의 "커밋 보류"보다 나은 길. 한 번 더 쓰이면 wrap 스킬에 넣을 것.

**과정 — 막힌 것 / 마찰**:
- 한글 파일명을 PowerShell 스크립트에 넘기느라 한 번 돌았다(스크래치로 복사해 해결). PS 실행 정책도 `-ExecutionPolicy Bypass`가 필요했다.
- 툴팁을 띄웠는데 안 보였다 — 스킬트리 창 자체가 닫혀 있었다(부모 체인 `activeSelf`를 찍고 알았다).

**AI 협업 체크**:
- 사용자가 켜 둔 플레이모드를 내가 껐다(씬 저장 때문). 묻지 않고 진행했고 보고에 적었다.
- 설명 칸이 93→73px로 줄어 3줄 초과 설명은 자동 축소된다 — 현재 최장 2줄이라 괜찮다고 판단.

## 세션 3 — 블루베리 디펜스 (출시 전 번역 · 간체 중국어)

**목표**: 현재 번역 상태를 재고 한/영/중(간체) 계획을 세워 실행.

**한 일**: 간체 로케일·464키 초벌·폰트 2종(Z Labs 16px 도트 + Noto Sans SC) 추가. 스킬트리 일반 노드 설명을 `perLevel`에서 짓게 바꿈(47개 중 11개가 틀린 숫자였음). en·zh 용어·어투 검수 49줄. 설정·레벨업 등 7개 화면을 zh 캡처로 확인.

**과정 — 잘 된 것**:
- 기계 검사가 0건일 때 "나란히 읽기 + 에셋 대조"를 따로 돈 덕에 **키 없는 노드 4개·틀린 숫자 11개**를 찾았다. 메모리 `translation-audit-needs-side-by-side`가 제 몫을 했다.
- 폰트 후보를 고르기 전에 주 폰트의 **도트 격자(16)** 부터 잰 것이 기준이 됐다. 사용자는 추천 한 줄로 결정했다.
- 굵기는 0/2/3/4를 한 장에 렌더해 한 번에 골랐다.

**과정 — 막힌 것 / 마찰**:
- 한자 정렬을 **폰트 수치로** 맞췄다가 제목이 6px 가라앉았다. 사용자가 두 번 짚었다(찌부 · 우하단 쏠림). 잉크로 재니 손대기 전이 정답이었다.
- 그 보정 때문에 만든 "굽기 + Static" 도구가 **언어 이름(로케일 에셋) 글자를 빠뜨려 두부**를 냈다. 보정을 없애자 도구도 필요 없어져 지웠다 — 구조가 복잡해진 원인이 틀린 보정이었다.
- 다른 세션이 크레딧·업적 파일을 동시에 고치는 중이었다. 시작 때 `git status`·수정 시각을 봐서 겹치지 않았다.

**AI 협업 체크**:
- 추측으로 진행: 영어 '연계' 용어를 계획서의 linked에서 Synergy로 바꿈 · zh 난이도 칸만 줄바꿈 · 원어민 검수 없이 초벌 — 전부 HANDOFF 확인 대기에 적음.
- 사용자가 짚어준 것: 한자가 찌부돼 보임 / 중앙이 안 맞음 / 두부 — 셋 다 내가 만든 캡처 검증이 못 잡았다. 굽기 방식을 바꾼 뒤 **이미 본 화면(설정)을 다시 안 찍었다.**
- 노션 카드 본문을 유니코드 이스케이프로 보냈다(칸반 스킬이 금지). 되읽기로 깨짐 0 확인.

**다음에 바꿀 것**:
- **[→ Kit]** 폰트 위치·크기는 글리프 수치가 아니라 캡처 잉크로 잰다 — `skills/unity-mcp` 체크리스트 6번 + BlueberryDefense `CLAUDE.md` §5-1에 반영함.
- 공용 에셋(폰트)을 바꾸면 그전에 찍었던 화면을 **전부 다시** 찍는다(이번엔 트리 하나만 다시 봤다).

## 세션 4 — 블루베리 디펜스 (크레딧 창 · 엔딩)

**목표**: 출시 막바지 남은 일(크레딧 창 · 엔딩 크레딧 · 번역 · QA) 중 크레딧 창과 엔딩.

**한 일**: 타이틀 블루베리 버튼 → 크레딧 창(명단은 `CreditsContent` 프리팹 한 곳, 엔딩과 공유). 광활한 우주 어려움 클리어 → 블루베리 군집체 등장 → 격파 → 암전 → 이야기 → Game Cleared! → 크레딧 → 타이틀. 음원·폰트·VFX 라이선스를 웹으로 조사해 인게임 표기 의무 없음을 확인하고, 폰트 OFL은 빌드 폴더에 자동 복사. 사용자가 두 번(한/영) 직접 보며 피드백.

**과정 — 잘 된 것**:
- 사용자가 "돌려 봐, 내가 볼게"라고 하면 정상 속도 + 자동 타격기로 무인 재생하고 백그라운드 타이머로 끝을 기다리는 방식이 잘 맞았다. 매번 세이브 백업 → 복원(+ `SaveStore` 캐시 비우기)까지 한 세트로.
- 판 그림 알파를 재서 마스크를 판 안쪽에 거는 것(§5-1 규칙)이 한 번에 맞았다.

**과정 — 막힌 것 / 마찰**:
- 크레딧 명단을 `Credits.txt` 런타임 조립으로 만들었다가 사용자가 "씬에서 고칠 수 있게"로 뒤집었다. 사용자는 **눈에 보이는 걸 에디터에서 직접 고치는 쪽**을 원한다.
- `.cs`를 고친 뒤 `assets-refresh` 없이 플레이모드에 들어가 옛 코드로 테스트했다(군집체가 땅에 박힘). unity-mcp 스킬에 있는 규칙인데 스크립트 수정 → 에셋 작업 → 플레이 순서 사이에서 빠졌다.
- 테스트 편의로 보스 없이 `GameClear()`를 부른 걸 미리 말하지 않아서, 사용자가 "군집체가 보스보다 먼저 뜬다"고 걱정했다.
- 문구 연출을 잘못 읽었다: "A, B 뜨고"를 한 화면 두 줄로 만들었는데 순차 두 화면이었다. "반드시 원형"은 테두리까지 둘러 너무 땡글하게 만들었다.
- 노션 카드 생성에 유니코드 이스케이프를 썼다(금지 규칙). 되읽어 오타는 없었다.
- wrap 때 병렬 세션 3개가 같은 작업 트리를 쓰고 있어 커밋을 못 했다 — 업적 세션도 같은 이유로 보류. 파일 단위로 서로의 변경이 얽혀 한 세션 몫만 떼면 컴파일이 안 된다.

**AI 협업 체크**:
- 추측으로 진행: 엔딩이 "깰 때마다" 재생 · 클리어 기록을 엔딩 전에 저장 · 군집체 체력(처음엔 보스와 같게 → 사용자가 2배로) · 영어 문구 초안. 전부 HANDOFF 확인 대기에 적음.
- 사용자가 잡아준 것: 크레딧 편집 방식, BGM 출처(전부 Juhani Junkala), 이야기 두 화면, 문구 속도, 그림 윤곽, 격파 후 3초 대기, 영어 모드의 한국어 제목, 명칭(어머니 → 군집체).
- 잘 된 자율 결정: 봇 실행 중엔 엔딩을 건너뛰게 한 것, 엔딩 중 스포너 정지·플레이어 무적.

**다음에 바꿀 것**:
- 테스트 지름길(상태를 코드로 건너뛰기)을 쓰면 **사용자가 보기 전에** 무엇을 건너뛰었는지 한 줄로 먼저 말한다.
- 병렬 세션이 한 작업 트리를 쓰면 wrap 커밋이 서로 막힌다 — 기능이 끝날 때마다 그 세션이 바로 커밋하거나 세션마다 worktree를 쓰는 방안을 사용자와 정할 것(결정 사항이라 키트 반영은 보류).

## 세션 4 — 블루베리 디펜스 (밸런스 루프 22~28회차)

**목표**: `/loop /balance-loop 20 17:00` — 보스 버그 수정 후 새 기준선으로 9목표 정주행 판수를 2~4판에 맞추기.

**한 일**: 22~28회차 7회 측정·판정(00:30~14:57). 해변 보통 4.67→2~3판(19층 벽 완화, 사용자 지시). 우주 어려움은 비행선→엘리트 교체 세 번이 전부 먹혔다(어려움에서만 비선형으로 센 적). 우주 쉬움은 구성·물량·간격·UFO 벽 여섯 가지가 전부 무반응 → 사용자가 체력·속도 배율로 결정. 쉬움 보스 피해 200→100(한 방 즉사 구조 발견). 보고서가 미측정 서열을 "0/8"로 그리던 버그 수정.

**과정 — 잘 된 것**: 사용자 질문("서열이 왜 악화됐어?", "농장 쉬움 왜 4판?")마다 **데이터 원천부터** 확인했다 — 전자는 미측정이 0으로 찍힌 표시 버그, 후자는 94개 에셋 지문 대조로 "아무것도 안 바뀜"을 먼저 확정하고 나서 보스 한 방 구조를 찾았다. 측정 중 사용자 지시는 에셋에 바로 안 쓰고 `plannedNext`에 쌓았다가 측정 끝에 한 번에 적용.

**과정 — 막힌 것 / 마찰**: ① 24회차에 물량과 간격을 같이 올려 **스폰 창 길이가 그대로**였다 — XP 선물만 됐고 사용자가 짚었다. ② 23회차 간격 축소 셋 중 둘이 무리 스폰 층이라 **무효**였는데 네 회차 뒤에야 발견. ③ 종료 알림이 48분 늦게 도착해 루프가 멈춰 있었다(보조 깨우기도 안 울림). ④ Unity 정지 5회 — 감시자가 전부 복구했지만 회차마다 +10~30분.

**AI 협업 체크**:
- 추측으로 진행: 인자 `20`을 "20회 더"로 · 22회차부터 probe 생략(19~21회차 관행 승계) — 이 둘이 합쳐져 서열이 8회차 동안 미측정인 걸 사용자가 보고서에서 먼저 봤다.
- 사용자 지적: "물량 늘리면서 간격도 줄였어? 안 그러면 공짜 경험치" — 내 모델에 없던 축.
- 잘 한 것: UFO 벽이 반증 조건(피해 0)에 걸리자 손잡이를 더 돌리지 않고 권한 밖 결정(스탯)으로 넘겼다.

**다음에 바꿀 것**:
- 물량 × 간격 · 무리 스폰의 `burstRest` 규칙을 `balance` 스킬 §3 "변경 크기"에 트리거와 함께 넣음(프로젝트 스킬 — 키트 대상 아님).
- 손잡이를 바꾸기 전에 **그 필드가 그 층에서 실제로 쓰이는지**(분기 조건)를 먼저 본다 — 이미 balance §3 0단계 "손잡이가 실제로 먹히는지"에 있는 규칙인데 안 지켰다. 새 규칙이 아니라 절차 문제.
