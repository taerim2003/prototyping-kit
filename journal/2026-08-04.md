# 2026-08-04

## 세션 1 — 블루베리 디펜스 (세션 28)

**목표**: 칸반 확인 후 범위 확정. 실제로는 **사용자 피드백 왕복**으로 흘렀다 — 휘두르기 손맛, 진화 조합 미리보기, 갈림길 UI, 진화 파워 조정.

**한 일**:
- **진화 조합 미리보기** — 갈림길 진화 카드에 `(스킬)+(연계)` 짝을 루트당 하나씩. 아이콘 46px, 여러 줄로 펴고 HUD 틀을 깔았다.
- **넉백 3차 재작업** — 순간이동 → 등속 → **EaseOutCubic**. "드드드 떨림"의 범인은 박치기 돌진(`AdvanceLunge`)이 매 프레임 x를 `holdBaseX + sin곡선`으로 덮어쓰는 것이었다. 밀리는 동안 전진·박치기·돌진을 전부 정지시켜 해결.
- **기절 버그** — 구현은 돼 있었고, 넉백 0.6초가 기절 0.5초를 통째로 먹고 있었다(`slowTimer` 감소가 넉백 분기보다 먼저).
- 휘두르기 피해 20→12(DPS 13.3→8.0), PATH1 흡혈, 시전 시 노란 범위 사각형.
- 진화 파워 3건: 대형 오브(관통 무한 + 쿨 ×0.75→×1.6) · 매사냥(발수 절반) · 쓸어치기(쿨 ×3).
- 갈림길 UI만 세로 카드 2장 — 씬은 안 고치고 코드로 전환/복원.

**과정 — 잘 된 것**:
- **대조군을 넣은 검증이 실제로 여러 번 걸러냈다.** 미리보기 첫 배치는 카드 밖으로 33px 나갔는데 대조군이 `insideCard=False`로 잡아줬고, 이징은 easeIn 대조군이 `frontLoaded=False`로 나와 검사가 살아 있음을 보여줬다. "전부 통과"만 봤으면 못 믿었을 것.
- **씬 수치를 실측하고 나서 배치를 정했다.** 설명 박스가 90px인데 한 줄 텍스트는 37px(TopLeft)이라 바닥 53px가 빈다 — 이걸 재고 나서야 올바른 위치가 나왔다. 추측으로 짰으면 두 번 고쳤다.
- **넉백을 값이 아니라 원인으로 접근했다.** "속도 올려주세요"에 상수만 만졌으면 떨림은 그대로였을 것.

**과정 — 막힌 것 / 마찰**:
1. 🔴 **UI 세로 전환의 범위를 통째로 잘못 잡았다.** 사용자는 "레벨업 또는 진화 둘 중 선택하는" **갈림길 하나**를 말했는데, 나는 "레벨업/진화 선택지 UI"를 *선택지 UI 전체*로 읽고 **씬의 카드 3장을 다 고쳐 저장**했다. 되돌리고 재작업.
   → `LevelUpUI` 하나가 **세 화면**(레벨업 3택 / 갈림길 2택 / 진화 대상 선택)을 그린다는 걸 알고 있었는데도, "어느 화면인가"를 안 물었다.
2. 🔴 **`optionOutlines`를 "미배선"으로 오판해 사용자에게 보고했다.** 에디트모드에서 `null`이라 배선 누락이라 결론지었는데, 실제로는 `Awake`에서 코드로 만드는 배열이라 런타임엔 정상이다.
   → **`unity-mcp` 스킬에 "에디트모드에서 런타임 필드를 읽어 검증하지 말 것"이라고 적혀 있는데도 어겼다.** 읽고도 어긴 케이스.
3. **"진화 아이콘 가시성"을 진화 *카드* 강조로 오해**했다. 사용자가 두 번 되짚어 준 뒤에야 조합 미리보기 아이콘 얘기인 걸 알았다.
4. `unity-mcp` 스킬을 세션 중반에야 열었다 — `script-execute`를 이미 여러 번 쓴 뒤였다.

**AI 협업 체크** *(코드 버그 말고 과정 레벨로)*:
- **물어봤어야 하는데 추측**: UI 세로 전환의 대상 화면. 마찰 1이 그대로 이 항목이다. 기능(세로로, 아이콘 아래) 은 명확했고 **대상만** 모호했는데, 그 하나를 안 물어서 씬을 고쳤다 되돌렸다.
- **유저가 명시했어야 했던 것**: 딱히 없다. 되짚어 보면 첫 요청에도 "그 보물상자때 등장하는 UI"의 의도가 읽힌다 — 내가 넓게 읽은 쪽이다.
- **잘 결정한 것**: 넉백을 원인 규명으로 접근한 것, 매 검증에 대조군을 넣은 것, 씬을 고치지 않고 코드 전환/복원으로 갈림길 레이아웃을 만든 것(되돌리기가 공짜가 됐다).

**다음에 바꿀 것**:
- **[→ Kit] "UI를 고치기 전에 그 코드가 그리는 화면을 열거한다."**
  한 컴포넌트가 여러 화면을 그리는 건 흔하다(`LevelUpUI` = 3화면). 트리거가 자명하다 — *UI 코드를 열 때*. 규범("잘 확인하자")이 아니라 **절차의 한 단계**로 박아야 지켜진다.
- 마찰 2는 **새 규칙을 만들 게 아니다.** 이미 스킬에 적혀 있고 읽고도 어겼다. 규칙 추가 대신, 에디트모드 검증 결과가 "없다/null"로 나오면 **보고 전에 그 필드가 어디서 채워지는지 grep 한 번**을 붙인다 — 이것도 절차 한 단계.

**메모**:
- 세션 27(파이프라인 정비) 일기가 누락된 것 같다. 8/2 파일이 세션 26에서 끝나고 8/3 파일이 없다. → **아래 세션 2로 추가됨.**
- 칸반에 사용자가 세션 중 "뉴캐릭터 공격 이펙트" 카드를 새로 올렸다 — 세션 중간에도 보드를 한 번 더 보라는 `kanban` 스킬 지침이 실제로 유효했다.
- 휘두르기 아이콘은 배선 문제가 아니라 **`activeIcons` 배열 길이가 9인데 스킬이 10종**이라 자리 자체가 없다. 아트가 나오면 배열부터 늘려야 한다.

---

## 세션 2 — 블루베리 디펜스 (세션 27, 파이프라인 병렬 세션)

**목표**: 게임을 만드는 게 아니라 **개발 파이프라인 자체를 외부 사례와 대조해 고치는** 세션. 사용자가 "잘하는 사람들은 어떻게 하는지 찾아서 내 것에 접목해보자"로 열었다.

**한 일**:
- 외부 조사 — Claude Code 공식 best practices · 스펙주도 3종(spec-kit/BMAD/Agent OS) · self-improving agents. 우리 방식을 **3층(지식 / 작업흐름 / 학습)** 지도로 위치 파악.
- **`CLAUDE.md` 244→159줄(−35%).** 절차를 스킬 3종으로 분리 — `kanban`(프로젝트) · `unity-mcp`·`wrap`(키트 원본 + `~/.claude/skills/`에 **정션**).
- **서브에이전트 = 안 쓰기로 확정**, 재검토 트리거 3개를 정함.
- **일기 34편의 "AI 협업 체크" 전수 집계** → 처방(착수 전 대상 선언) 도출, 프로젝트·키트 CLAUDE.md 양쪽에 반영.
- 메모리 6개 감사 — `project` 3개에서 낡은 상태 서술 제거.

**과정 — 잘 된 것**:
- **사용자가 붙여준 5/25 노션 기록이 논점을 통째로 뒤집었다.** 나는 "서브에이전트 금지는 논의된 적 없는 공백"으로 읽고 새 제안을 들고 왔는데, 실제로는 에이전트 팀을 써보고 접은 결정이 있었다. 게다가 그 기록의 핵심(*"초기화만 30% 먹음"*)은 에이전트의 결함이 아니라 **우리 문서의 무게**를 가리키고 있었다 — 저울을 치웠지만 무게는 남아 있었다.
- **실측이 내 제안을 두 번 꺾었다.** ① 서브에이전트 — 코드베이스가 64파일이라 grep으로 충분하고, **오늘 세션 자체가 증거**였다(72KB·52KB 출력을 파일 저장 + 2차 grep으로 처리). ② HANDOFF 슬림화 — "세션 로드의 81%"라는 크기만 보고 추천했는데, 성격이 달라(자동 주입 vs 1회 Read) 같은 처방이 안 맞았다. 직전에 내가 근거까지 대며 민 건이었다.
- **34편 집계가 질문을 바꿨다.** "개선되고 있나"를 재려다 **"무엇이 변수인가"**가 나왔다 — 추측 발생률이 초기 83% → 중기 14% → 후기 44%로 단조가 아니었고, 급락 구간이 **전부 계획된 리팩토링 세션**이었다. 실력이 아니라 **작업 유형**이 결정한다. 취약 구간은 즉흥 피드백 왕복.
- **메모리끼리 모순하는 걸 찾았다.** `verify-handoff-claims-before-citing`이 "이 주장은 틀렸다"고 기록한 바로 그 문장이 `upgrade-system-pivot-skilltree`에 그대로 살아 있었다. **사고를 기록한 메모리는 만들었는데 사고의 원인은 안 고쳤다.** 둘 다 매 세션 주입된다.

**과정 — 막힌 것 / 마찰**:
- 🔴 **검증 스크립트가 방금 만든 파일을 깨뜨렸다.** PowerShell 5.1의 `Get-Content`가 BOM 없는 UTF-8을 CP949로 읽어, 되쓰는 순간 한글이 통째로 mojibake가 됐다. **그 함정을 바로 그 파일에 적어놓고 밟았다**(파일엔 "훅 스크립트는 ASCII 전용 — no-BOM PS가 한글을 깨뜨림"이라고 적혀 있었다. 같은 함정의 다른 얼굴을 못 알아봤다).
- 🔴 **`<details>`로 감싸면 되는 줄 알았다.** CLAUDE.md에서 절차를 접어두려 했는데, 접기는 사람 눈에만 접히고 **모델은 전부 읽는다.** 목적이 0% 달성되는 편집을 하고 나서야 알아챘다.
- 🔴 **병렬 세션의 존재를 `/wrap` 4단계에서야 알아챘다.** 미커밋 403줄을 "사용자 작업"으로 단정했는데 실제로는 다른 Claude 세션의 산출물이었다(파일 수정 시각을 보고서야 뒤집혔다). 커밋 범위를 물어본 덕에 남의 작업이 내 커밋 메시지에 묻히는 건 막았다.
- **칸반 조회가 불완전한 걸 보고도 진행했다.** `has_more=true` 경고를 읽고도 7장을 전부로 취급했다. 병렬 세션은 9장을 봤다. `kanban` 스킬에 "무시하면 안 되는 경고"라고 적어둔 그것이다.
- **분량 예측이 또 빗나갔다.** "126줄 남는다" → 실제 182줄. 세션23에서 4배 틀린 것과 같은 실수(이번엔 포인터 줄을 안 셌다).

**AI 협업 체크** — 추측 판정: **●**
- *추측으로 진행한 것*: **있다.** 미커밋 403줄의 출처를 확인 없이 "사용자 것"으로 단정하고 0단계 보고에 그렇게 적었다.
- *사용자가 잡아준 것*: **세 번.** "너무 앞서 나간다"(페이스) · "하나씩 논의하자"(인지 부하) · "그거 싫어, 귀찮아"(범위). 셋 다 **내가 벌린 것을 좁히는** 지적이었다. 논의형 세션에서 나는 갈래를 계속 늘리는 쪽으로 치우친다.
- *잘 결정한 것*: 커밋 전에 멈추고 범위를 물은 것. 전체 스테이징이 기본 흐름이었는데 남의 403줄이 섞여 있었다.
- *잘 결정한 것 2*: 내 제안을 실측으로 두 번 꺾은 것.

**다음에 바꿀 것**:
- **[→ Kit] 메모리엔 규칙만 넣고, 변하는 것은 "어디서 확인하는지"만 적는다.** `feedback` 타입은 24일이 지나도 안 낡았고 `project` 타입만 낡았다 — 차이는 **그 시점 상태가 섞였는지**였다.
- **[→ Kit] 파일을 PowerShell로 왕복시키지 않는다.** 검증은 읽기 전용으로 짠다. (`unity-mcp` 스킬에 반영 완료.)
- **병렬 세션 가능성을 세션 시작에 확인한다.** `git status`의 파일 수정 시각을 보면 바로 드러나는데, 오늘은 끝에서야 봤다.
- **`has_more` 같은 경고는 보면 멈춘다.** 보고도 진행해서 카드 2장을 놓쳤다.

**메모**:
- 서브에이전트 재검토 트리거 — ① 코드베이스가 수백 파일 ② 탐색↔읽기 사이클이 한 작업에서 3회 이상 ③ 낯선 서드파티 코드를 파야 할 때.
- 남은 논의: **층2**(spec-kit식 단계 강제를 우리 흐름에 접목) · **HANDOFF/ARCHITECTURE**(합 47KB, 세션 로드의 81%)를 어떻게 볼 것인가.
- 5/25 "에이전트 팀 중지" 기록은 노션에만 있다. 사용자가 "접은 것 목록" 문서는 만들기 싫다고 해서(귀찮음) **이 일기가 그 대용**이다.
- 스킬 3종이 전부 첫 실사용까지 검증됐다 — `/kanban`(사용자가 직접 호출) · `unity-mcp`(목록 등록 확인) · `/wrap`(이 세션 종료). 정션은 재시작 없이 즉시 인식됐다.
- **오늘 두 세션이 같은 파일(CLAUDE.md·HANDOFF)을 실시간으로 주고받았다.** 내 처방을 저쪽이 읽고 자기 사고를 반영해 보강해 놓은 게 보인다 — 충돌은 났지만 결과는 양쪽 다 나아졌다.
