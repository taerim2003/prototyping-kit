# 2026-07-06

## 세션 1 — 블루베리 디펜스 시작 준비 (메타: Notion MCP 트러블슈팅)

**목표**: 새 프로토타입 "블루베리 디펜스" 시작 — 노션에서 기획 문서 참조 예정.

**한 일**:
- Notion MCP가 이 세션에 로드되지 않는 문제 진단 및 해결.
- `C:\Users\User\.mcp.json` 에 Notion MCP 설정 저장 (settings.json 대신 별도 파일로).
- ONBOARDING.md에 "세션 시작 시 Notion MCP 트러블슈팅" 섹션 추가.

**원인 분석**:
- 6월 9일 일기엔 "Notion MCP 이미 Connected"로 기록돼 있었음.
- 현재 `~/.claude/settings.json`에 `mcpServers` 키가 없었음.
- **가장 유력한 원인**: Claude Code 업데이트 또는 `/config`·UI 설정 변경 시 settings.json이 통째로 재작성되면서 비표준 키가 제거됨.
- settings.json에 `mcpServers`를 직접 쓰려 했으나 스키마 검증에서 "Unrecognized field" 오류 발생 → settings.json은 공식적으로 `mcpServers`를 지원하지 않음.

**해결책**:
- `C:\Users\User\.mcp.json` 사용 — settings.json과 별개 파일이라 업데이트·재작성 영향을 받지 않음.
- 토큰 형식: `@notionhq/notion-mcp-server`는 `OPENAPI_MCP_HEADERS` 환경변수에 JSON 문자열로 Bearer 토큰을 넘겨야 함 (구형 `NOTION_TOKEN`과 다름).

**다음에 바꿀 것**:
- **노션 MCP가 안 되면 먼저 `~/.mcp.json` 확인** — settings.json을 뒤지지 말 것. 설정 위치가 바뀌었음.
- **토큰을 채팅에 공유하지 않도록 안내** — 사용자가 이번에 토큰을 대화창에 붙여넣음. 토큰 입력은 파일에 직접 쓰는 방식으로 유도할 것. (토큰 폐기 후 재발급 권장)

**메모**:
- Claude Code 재시작 후 Notion MCP 연결 여부 확인 필요.
- 블루베리 디펜스 기획 문서 열람은 MCP 연결 확인 후 진행 예정.

## 세션 2 — 블루베리 디펜스 온보딩 + SESSION_ZERO + GitHub 연결

**목표**: 노션 기획 확인 후 Unity 프로젝트 생성, 온보딩 절차대로 키트 문서 채우고 GitHub까지 연결.

**한 일**:
- 노션에서 GDD 원본 확인, `d:\unity\BlueberryDefense\` Unity 6(URP 2D) 프로젝트 생성 확인.
- 키트 템플릿 복사 + CLAUDE §5 / GDD §1~2 / HANDOFF 채움, git init + 첫 커밋.
- SESSION_ZERO.md로 스코프 확정: 액티브/패시브 9종→5종, 젬 7종→4종으로 컷, Must/Should/Won't Have 분리.
- 그래픽 방침: 이펙트=에셋스토어(웹서치로 실제 존재 확인), 캐릭터=직접 그리기로 결정.
- gh CLI 설치(winget) 후 GitHub private repo(`taerim2003/BlueberryDefense`) 생성·push.
- ONBOARDING.md/README.md에 SESSION_ZERO 단계·경로 주의·gh CLI 체크 반영 ([→ Kit], 아래 참고).

**과정 — 잘 된 것**:
- 노션 원안(스킬 18종+젬7+스테이지20)을 그대로 다 넣지 않고, 사용자와 대화하며 단계적으로 협상해 실현 가능한 범위로 컷함. "Must Have 검증 후 Should Have 확장"이라는 순서를 명확히 하니 사용자도 납득.
- 그래픽 소싱 질문에 추측 대신 실제 웹서치로 에셋스토어 현황을 확인하고 하이브리드안(이펙트=에셋/캐릭터=직접)을 근거와 함께 제안.

**과정 — 막힌 것 / 마찰**:
- **온보딩 완료 후 바로 GitHub 연결로 넘어가려다 SESSION_ZERO를 빠뜨림.** 사용자가 "세션 제로 관련한거 여기서 하는게 아니야?"라고 지적해서 알아챔 — `CLAUDE.md` §7-0은 SESSION_ZERO를 요구하는데 `ONBOARDING.md`엔 그 단계가 아예 없었음 (두 문서가 서로를 참조 안 함).
- `ONBOARDING.md`가 `f:\Prototyping\`을 하드코딩하고 있는데 이 기기의 실제 레이아웃은 `d:\unity\` — 경로 불일치를 그때그때 수동으로 매핑함.
- `gh` CLI가 이 기기에 없었고, winget 첫 설치 시도가 "install server not responding"으로 실패 → 재시도해서 성공 (원인 불명, 사용자가 관리자 권한 승인 후 재시도가 통함).

**AI 협업 체크**:
- AI가 물어봤어야 하는데 추측으로 진행: 온보딩 6단계(인계)를 SESSION_ZERO 없이 그대로 GitHub 연결로 넘기려 함 — 사용자가 잡아줌.
- 유저가 명시했어야 했는데 암묵적으로 넘어간 전제: 딱히 없음, 스코프 컷·그래픽 방침 모두 명시적으로 합의.
- AI가 자율적으로 잘 결정한 것: 스킬/젬 개수 협상을 축(AoE/컨트롤/도트/버스트 등)으로 정리해 제안한 것, 그래픽 리서치를 근거로 제시한 것.

**다음에 바꿀 것**:
- **[→ Kit] 완료**: `ONBOARDING.md`에 SESSION_ZERO를 명시적 6단계로 추가, 경로가 기기마다 다를 수 있다는 주의문 추가, GitHub 연결 시 `gh --version` 사전 확인 문구 추가. `README.md` 요지 요약도 동기화.
- 다음 온보딩 때 위 반영이 실제로 마찰을 줄이는지 확인.

**메모**:
- BlueberryDefense repo: https://github.com/taerim2003/BlueberryDefense (private)
- 다음 세션 = SESSION_ZERO Must Have #1(블루베리 스폰·이동·데미지)부터 코딩 시작.
